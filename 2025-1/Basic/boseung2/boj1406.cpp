#include <bits/stdc++.h>
using namespace std;

int main(void) {
  ios::sync_with_stdio(0);
  cin.tie(0);
  
  string init;
  cin >> init;
  list<char> list;
  for(auto c : init) list.push_back(c);
  auto cursor = list.end();
  cin << q;
  while(q-- ){
    char op;
    cin >> op;

    //구현예정
  }

}